#include "Observer.hpp"

#include <iostream>
//#include <string>


/*void Observer::update(std::string change)
{
	if (change == "camera") {
		use();
		
		if (scene->shader[i].getType() == ShaderType::AMBIENT)
		{
			scene->shader[i].sendUniform("modelMatrix", scene->object[i].getMatrix());
			scene->shader[i].sendUniform("viewMatrix", scene->camera[0].getCamera());
		}
		else if (scene->shader[i].getType() == ShaderType::DIFFUSE)
		{
			scene->shader[i].sendUniform("modelMatrix", scene->object[i].getMatrix());
			scene->shader[i].sendUniform("viewMatrix", scene->camera[0].getCamera());
		}
		else if (scene->shader[i].getType() == ShaderType::SPECULAR)
		{
			scene->shader[i].sendUniform("modelMatrix", scene->object[i].getMatrix());
			scene->shader[i].sendUniform("viewMatrix", scene->camera[0].getCamera());
			scene->shader[i].sendUniform("viewPos", scene->camera[0].getPosition());
		}
		else if (scene->shader[i].getType() == ShaderType::PHONG)
		{
			scene->shader[i].sendUniform("modelMatrix", scene->object[i].getMatrix());
			scene->shader[i].sendUniform("viewMatrix", scene->camera[0].getCamera());
			scene->shader[i].sendUniform("viewPos", scene->camera[0].getPosition());
		}
	}
	else if ("projection") {

	}

	std::cout << "hello\n";
	
}*/